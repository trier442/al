name: 2027 언어와 매체 최종 검증 완료

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "2027 언어와 매체 42개 전면 재작성·전수 검증·WordPress 게시가 완료되었습니다."
