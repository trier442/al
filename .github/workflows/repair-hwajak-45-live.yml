name: Repair hwajak 4 and 5 live posts

on:
  push:
    branches: [main]
    paths:
      - 'scripts/repair-hwajak-45-live.mjs'
      - '.github/workflows/repair-hwajak-45-live.yml'
  workflow_dispatch:

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Build current hwajak source
        run: |
          node scripts/standardize-hwajak-pages.mjs
          node scripts/fix-hwajak-pages.mjs
          if [ -d scripts/hwajak-overrides ]; then
            for OVERRIDE in scripts/hwajak-overrides/2027-suteuk-hwajak-*.html; do
              [ -e "$OVERRIDE" ] || continue
              cp "$OVERRIDE" "wordpress-content/$(basename "$OVERRIDE")"
            done
          fi
          node scripts/apply-hwajak-text-patches.mjs
          for PATCH in scripts/hwajak-single/*.mjs; do
            [ -e "$PATCH" ] || continue
            node "$PATCH"
          done
      - name: Validate 4 and 5
        run: |
          node scripts/validate-hwajak-single.mjs wordpress-content/2027-suteuk-hwajak-s04a.html
          node scripts/validate-hwajak-single.mjs wordpress-content/2027-suteuk-hwajak-s04b.html
          node scripts/validate-hwajak-single.mjs wordpress-content/2027-suteuk-hwajak-s05a.html
          node scripts/validate-hwajak-single.mjs wordpress-content/2027-suteuk-hwajak-s05b.html
      - name: Repair WordPress publication
        env:
          WP_URL: ${{ secrets.WP_URL }}
          WP_USERNAME: ${{ secrets.WP_USERNAME }}
          WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
        run: node scripts/repair-hwajak-45-live.mjs
